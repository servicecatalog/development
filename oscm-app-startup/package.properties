# Copyright FUJITSU LIMITED 2016
oscm-app-startup.war@WEB-INF/classes/ = ${result.work.classes.dir}[**/*]

# JSF UI
oscm-app-startup.war@ = ${project.dir}/WebContent/[**/*]
oscm-app-startup.war@META-INF = ${javares.dir}/META-INF/[*.MF] | replace
oscm-app-startup.war@WEB-INF/lib = ${workspace.dir}/libraries/apache-beanutils/javalib/[*.jar]
oscm-app-startup.war@WEB-INF/lib = ${workspace.dir}/libraries/apache-digester/javalib/[*.jar]
oscm-app-startup.war@WEB-INF/lib = ${workspace.dir}/libraries/jboss-richfaces/javalib/[*.jar]
oscm-app-startup.war@WEB-INF/lib = ${workspace.dir}/libraries/apache-logging/javalib[*.jar]

# Copyright FUJITSU LIMITED 2017
# Connector implementation

# JSF UI
oscm-app-common.war@ = ${project.dir}/WebContent/[**/*]
oscm-app-common.war@META-INF = ${javares.dir}/META-INF/[*.MF] | replace

oscm-app-common.war@WEB-INF/lib = ${workspace.dir}/libraries/jboss-richfaces/javalib/[*.jar]
oscm-app-common.war@WEB-INF/lib = ${workspace.dir}/libraries/apache-tomahawk/javalib/[*.jar]
oscm-app-common.war@WEB-INF/lib = ${workspace.dir}/libraries/apache-fileupload/javalib/[*.jar]
oscm-app-common.war@WEB-INF/classes = ${result.work.classes.dir}[**/*.class]
oscm-app-common.war@WEB-INF/classes = ${result.work.classes.dir}[**/faces-config.xml]
oscm-app-common.war@WEB-INF/classes = ${result.work.classes.dir}[**/MANIFEST.MF]
oscm-app-common.war@WEB-INF/classes = ${result.work.classes.dir}[**/interstage.skin.properties]
branches:
  only:
    - fb_web_integration_tests

language: java

jdk:
  - oraclejdk8
  
addons:
  firefox: "23.0" 

services:                                       
  - selenium

env:
  global:
    - ANT_OPTS=-Xmx4g
    - COMUPTERNAME=G02PLXNNOW29168
    - HOSTNAME=G02PLXNNOW29168

build:
  pre_ci_boot:                          
    image_name: servicecatalog/oscm
    image_tag: latest
    pull: true
  ci:
	- mvn -f development/oscm-portal-integrationtests/pom.xml clean package test
